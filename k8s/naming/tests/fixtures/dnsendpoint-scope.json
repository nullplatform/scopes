{
  "apiVersion": "v1",
  "kind": "List",
  "items": [
    {
      "apiVersion": "externaldns.k8s.io/v1alpha1",
      "kind": "DNSEndpoint",
      "metadata": {
        "name": "k8s-checkout-api-production-123456-dns",
        "namespace": "nullplatform",
        "labels": {"scope_id": "123456"}
      },
      "spec": {
        "endpoints": [
          {"dnsName": "checkout.example.com", "recordTTL": 60, "recordType": "A", "targets": ["10.0.0.5"]}
        ]
      }
    }
  ]
}
